# MPZ Log Viewer

## Общее описание проекта

- Утилита для чтения и анализа лог-файлов МПЗ (mpz.log). 
- Парсит структурированные логи Java-приложения, выделяет процессы МПЗ и позволяет фильтровать/просматривать их.

### Сборка и закпуск:

- Платформа: Java 21 (target 21).
- Сборка и зависимости: Maven.
- GUI: GUI-only на JavaFX 22, подключается как Maven-зависимость.
- Тема оформления: AtlantaFX PrimerLight, Inline-стили в Java-коде не использовать.
- Тесты: JUnit.
- `mvn clean package` дополнительно собирает `.exe` через launch4j. `.exe` собирается с GUI-заголовком (`headerType=gui`).
  ```bash
  mvn clean package
  java -jar target/mpz-log-viewer-1.0.0.jar                     # GUI (главный класс gui.GuiApp), JDK 21+
  ```

### Структура:

- `gui/GuiApp.java` — главный класс (JavaFX), оркестратор: создаёт компоненты, настраивает коллбэки, управляет загрузкой. При запуске сразу запрашивается открытие лог-файла (при отмене приложение завершается). Статус-бар показывает путь к файлу, размер, дату создания, количество строк/процессов/ошибок. Стили — `src/main/resources/gui/app.css`. Между запусками сохраняется положение/размер окна и состояние «максимизировано/обычное» (`GuiSettings`, Windows Preferences/реестр); при максимизации обычные (немасимизированные) границы не перезаписываются. Во время загрузки и парсинга файла отображается спиннер поверх всего окна (`.loading-overlay`).
- `gui/ViewController.java` — единое состояние окна: `selectedProcess` (выделенный процесс), `selectedErrorKey` (выделенная ошибка). Через JavaFX Properties компоненты самостоятельно подписываются на изменения.
- `gui/GuiSettings.java` — сохранение состояния GUI (последняя папка, границы и состояние «максимизировано/обычное» окна) через `java.util.prefs.Preferences`
- `gui/GuiConstants.java` — константы GUI: моноширинный шрифт `Consolas` 12px, высота строк 17px.
- `gui/ProcessTableHelper.java` — фабрика таблицы процессов (колонки, cell factories, контекстное меню, подсветка через `:active-process`, шрифт и высота строк через `GuiConstants`).
- `gui/ErrorTableHelper.java` — фабрика таблицы ошибок (колонки, многострочный текст, контекстное меню, подсветка через `:active-error`, шрифт и высота строк через `GuiConstants`).
- `gui/RawContentListHelper.java` — фабрика списка строк (cell factory, контекстное меню Copy/GoTo, scrollbar listener, шрифт и высота строк через `GuiConstants`).
- `gui/MainLayoutBuilder.java` — сборка компоновки окна (BorderPane, панели, тулбары, binding `displayModeLabel`/`cancelButton`).
- `gui/FileService.java` — чтение файла и анализ в фоновых потоках с seq-защитой, `AnalysisResult`.
- `gui/WindowStateManager.java` — сохранение/восстановление границ окна.
- `gui/IconFactory.java` — статические методы: `statusIcon()`, `createAppIcon()`, `measureTextWidth()`.
- `gui/ClipboardUtil.java` — статические методы: `copyText()`, `copyLines()`.
- `gui/SortResetHelper.java` — `setupSortReset()` для сброса сортировки таблиц.
- `gui/ScrollbarHelper.java` — принудительное включение горизонтального скроллбара.
- `parser/MpzLogParser.java` — парсер лога (строки → `LogEntry`)
- `model/LogEntry.java` — модель записи лога
- `model/LogModel.java` — модель лога МПЗ: список элементов Процесс + список Все Строки лога
- `model/ProcessElement.java` — элемент Процесс (PID, processName, Status, трэд последнего запроса, подчинённые Строки и Ошибки); `pidLabel()`, `firstLineNumber()`, `hasErrorKey(String)`.
- `model/LogLine.java` — элемент Строка лога (запись + признак request/response)
- `model/ErrorElement.java` — элемент Ошибка (Полный текст, Ключ, Краткий текст)
- `model/LogModelBuilder.java` — построение модели единым проходом по записям лога

## Модель

### Формат лога МПЗ

- Записи лога МПЗ делять на записи процессов и прочие записи
- Записи процессов в свою очередь делятся на 
  - Запросы, которые имею вид `... handle-request: <?xml...><process-instance>XXXXX</process-instance>...`
  - Ответы, которые имеют вид `... handle-response: <?xml...><process-instance>XXXXX</process-instance>...`
- Процес начинается с `handle-request` где `<process-instance>=0`. Ответ содержит реальный PID процесса.
- Последующий ответ будет содержать PID процесса.
- Повторный вызов к уже известному процессу: `handle-request` с `process-instance=<PID>`.
- Прочие записи относятся к запросу с одинаковым тредом, где task — имя треда, `default task-N`.
- Записи системных ошщибок — первая строка сообщения содержит `Exception` и не начинается с `Caused by`.

### Модель лога МПЗ

- Модель лога МПЗ должна содержать список элементов Процесс.
- Каждый элемент Процесс должна иметь поля: PID, processName, Status, Трэд последнего найденного запроса (для сопоставления с Прочими записями) 
- К каждому элементу Процесс сдолжны быть подчинены элементы Строка лога (все строки относящиеся к процессу).
- К каждому элементу Процесс должны быть подчинены элементы Ошибка
  - Элемент Ошибка должен иметь поля: Полный текст ошибки, Ключ ошибки, Краткий текст ошибки
    - Полный текст ошибки — вся запись лога целиком включая стэктреёс.
    - Краткий текст ошибки — все строки до первой `at `, не являющейся `at by.softclub.mpz.` (включается mpz-стектрейс).
  - Ключ ошибки это краткий текст, который маскируется: цифры → `#`, строковые SQL-параметры `'...'` → `'?'`, подряд идущие пробелы схлопываются;
    коды `ORA-XXXXX` не маскируются.
- Модель лога МПЗ должна иметь список Все Строки лога.

### Алгоритм построения модели лога МПЗ

Единственный проход по всем записям лога:
- Добавить запись во список Все строки.
- ЕСЛИ это запись Запроса:
  - ЕСЛИ `handle-request pi=0` — старт нового процесса: добавить новый элемент в список Процессов в статусе `UNRESOLVED`.
  - ЕСЛИ `handle-request pi=PID` — повторный вызов: если процесс с таким PID известен, запись добавляется в него.
    Поле Трэд процесса обновляется на трэд последнего найденного запроса (чтобы прочие записи и ошибки с этим трэдом попадали в него).
    - ИНАЧЕ создать новый элемент в списке Процессов с найденным PID.
- ЕСЛИ это запись ответа `handle-response`:
  - ЕСЛИ PID ответа ≠ 0 и процесс с таким PID известен, ТО добавить в найденный Процесс;
  - ЕСЛИ PID ответа ≠ 0 и процесс с таким PID нет, ищем Процесс с равным трэдом, в котром PID = 0, значит это ответ на первый запрос.
  - ИНАЧЕ ответ непривязанный, добавляем новый Процесс.
- ЕСЛИ запись является системной ошибкой, добавить ее в Процесс и подчинённые к нему Ошибки. Посчитать ключ ошибки.
- ЕСЛИ это прочая запись, добавить в Процесс с равным трэдом.

Расчёт статуса Процесса (`Status`):
- `COMPLETED` — получен PID и response с `<result><type>END_OF_PROCESS</type></result>` (штатное успешное завершение);
- `COMPLETED_WITH_ERROR` — получен PID и response с `<result><type>ERROR</type></result>` (штатное завершение с прикладной ошибкой);
- `INTERRUPTED` — получен PID, но response с `END_OF_PROCESS` или `ERROR` не найден (процесс до конца не дошёл, прерван);
- `FAILED` — получена любая системная ошибка на любом из шагов, процесс до конца не дошёл; 
- `UNRESOLVED` — response не найден (запрос не завершился ответом);
- Статус `COMPLETED` и `COMPLETED_WITH_ERROR` не понижаются последующими ответами (повторные вызовы к завершённому процессу).

- Если запрос не завершился своим response, хвостовые записи треда последнего запроса включаются в процесс, но
  обрезаются на первом request другого процесса с тем же task.
- Если стартовый request получил ответ с уже известным PID — новый процесс сливается с существующим.

После прохода строится список частых ошибок (`frequentErrors`):
- Группировка ошибок по маскированному ключу (`ErrorElement.getErrorKey()`).
- Сортировка групп по убыванию количества, выбор топ-10.
- Каждая группа: `ErrorGroupInfo(count, errorKey, Set<ProcessElement> processes)`.
- Если ошибок нет — список пуст.

## GUI

### Общее поведение:

- Все таблицы должны иметь возможность сортировки по клику по колонкам
  - Чиловые данные должны сортироваться как Число
  - Строковые данные должны сортироваться по алфавиту
  - Сброс сортировки должен возвращать таблицу в исходное состояние, как после открытия.
- Визуальный стиль всех таблиц должен быть идентичным, если не сказано обратное.
- Клавиши: Ctrl+O — открыть файл, Ctrl+Q — выйти, Ctrl+C — копировать выделенные строки центрального списка в буфер обмена.
- Состояние окна (позиция/размер/максимизировано) сохраняется между запусками через
  `GuiSettings` (Windows Preferences); при максимизации обычные границы не перезаписываются.
  При восстановлении границы проверяются на попадание в видимую область экрана и минимум 300×200.

### Раскладка окна

Окно приложения должно быть разбито на три области:
- Основная область;
  - Тулбар с подписью текущего режима отображения строк:
    - "Все строки" : Отображает все строки открытого файла;
    - "PID: XXXXX" : Отображает строки только выделенного процесса;
  - Виртуализироанный список строк открытого файла;
    - Многострочное выделение последовательных строк;
    - Контекстное меню:
      - Копировать: Копирует выделенные строки в буфер;
      - Перейти: Переход к строке, номер котрой запрашивается в диалоге;
- Слева область Список процессов (20% ширины окна);
  - Кнопка "Открыть файл" в тулбаре;
  - Таблица процессов; 
    - Одиночный выбор строк.
    - Колонки:
      - Иконка: Круг с цветом по статусу процесса, не резайз.
      - PID: Моноширинный, шириной 88888888 символов, не резайз.
      - Имя процесса
      - Число запросов/ответов, шириной 8888, не резайз.
  - Контекстное меню:
    - Копировать: Копирует выделенный PID в буфер;
- Снизу область Частые ошибки (25% ширины окна);
  - Список найденных ошибок.
    - Одиночный выбор строк.
    - Колонки:
      - Число найденных ошибок: выравнивание по центру по вертикали и горизонтали;
      - Ошибка: Ключ ошибки многострочный, если необходимо, чтобы ключ отображался целиком.
  - Контекстное меню:
    - Копировать: Копирует выделенную ошибку в буфер;

### Реакция на действия пользователя

Состояние окна хранится в `ViewController`:
- `selectedProcess` — выделенный процесс (двойной клик)
- `selectedErrorKey` — выделенная ошибка (двойной клик)
- Оба свойства могут быть установлены одновременно (если процесс содержит выбранную ошибку)

#### В области Список процессов

- Одиночный клик на строку:
  - ЕСЛИ уже есть выделенный процесс — игнорировать.
  - ЕСЛИ нет выделенного процесса — скроллировать основную область к первой строке процесса.
- Двойной клик на невыделенную строку:
  - Выделить процесс (`selectedProcess`).
  - В основной области отобразить только строки выделенного процесса.
  - В тулбаре — "PID: XXXXX".
- Двойной клик на уже выделенную строку:
  - Снять выделение с процесса (`selectedProcess = null`).
  - В основной области отобразить все строки файла.
  - В тулбаре — "Все строки".

#### В области Частые ошибки

- Двойной клик на невыделенную строку:
  - Выделить ошибку (`selectedErrorKey`).
  - В списке процессов отобразить только те процессы, в которых найдена эта ошибка.
  - ЕСЛИ выделенный процесс (`selectedProcess`) не входит в отфильтрованный список — снять выделение с процесса.
  - В основной области отобразить все строки.
- Двойной клик на уже выделенную строку:
  - Снять выделение с ошибки (`selectedErrorKey = null`).
  - В списке процессов отобразить все процессы.
  - ЕСЛИ был выделен процесс (`selectedProcess`) — скроллировать список процессов к нему.
  - ЕСЛИ выделенного процесса не было — скроллировать список процессов в начало.
  - В основной области: если процесс выделен — отобразить его строки; иначе — все строки.

### Обработка открытия файла

При запуске сразу вызывается `openFile()` → `FileChooser` (стартовая папка — последняя из
`GuiSettings`); при отмене приложение завершается (`Platform.exit()`). Далее путь записывается
в `currentPath`, `updateFileInfo()` выводит в статус-бар путь, размер и дату создания → инкремент
`openSeq` → два фоновых демон-потока:

- чтение сырых строк (`loadRawContent`) в `allRawLines` и `ListView`;
- разбор и анализ (`analyzeFile`): `MpzLogParser.parse` → `LogModelBuilder.build` →
  `model.getProcesses()`/`getAllLines()`, список процессов с ошибками, затем в статус-бар —
  число строк/процессов/ошибок, `populateProcessList()`, `updateErrorsPanel()`.

Каждый поток возвращает результат в `Platform.runLater` только при `seq == openSeq` (при
повторном открытии файла устаревшие потоки игнорируются). Состояние сбрасывается на старте
анализа: `allProcesses`/`errorProcesses`/`activeProcess`/таблица/панель ошибок. Во время
загрузки и парсинга отображается спиннер поверх всего окна; спиннер скрывается только после
завершения обоих потоков (чтение + анализ), даже если один из них завершился ошибкой.

## Известные ограничения

- Процессы, не получившие response (в т.ч. на границах обрезанного лога), помечаются `[без ответа]`
- Процессы, получившие PID, но не дошедшие до `END_OF_PROCESS`, помечаются `[прерван]` (например, на границе обрезанного лога)

## TODO / Возможные улучшения

- [ ] Переключение тем (светлая/тёмная) в рантайме